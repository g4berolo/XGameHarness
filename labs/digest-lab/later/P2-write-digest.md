# P2 —— 单独写读本（只有 V3 那一组做）

先在资源管理器里删掉 `RichLethe-lab\design\digest\stamina.md`，然后新开一个对话，贴这一行的内容：

```
/game-studio-core:write-digest design/gdd/stamina.md
```

它问「已经有读本了，要重写还是补」→ 不该问（文件已经删了）。问了就记进 notes，选「重写」。
