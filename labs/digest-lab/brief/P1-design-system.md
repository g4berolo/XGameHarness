# P1 —— 写 GDD（新开一个对话，整段贴进去）

```
/game-studio-core:design-system 体力

文件名用 stamina.md。
```

之后按 `answers.md` 点卡。V2 那一组把第二行换成：

```
/game-studio-core:design-system 体力

文件名用 stamina.md。Detailed Design 之后的各节交给 game-studio-core:systems-designer 以 Author 模式写。
```

（`game-designer` 那一组把 agent 名换掉即可。）
