# P3 —— 评审（读本写完之后，新开一个对话，贴这一行的内容）

```
/game-studio-core:design-review design/gdd/stamina.md（只报，不改任何文件）
```

它输出的整段评审：在转录里从「## Design Review」拖选到「Verdict」那一行，复制，
存到 `runs/<run-id>/review.md`（`collect.mjs` 不会替你抓对话）。
