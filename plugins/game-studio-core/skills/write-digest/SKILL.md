---
name: write-digest
description: "Write the 读本 (reader's edition) section into an existing design document or memo — a complete plain-language account of the same system, for someone on the project who does not work on this area."
argument-hint: "[path-to-doc] (e.g., design/gdd/social-contract.md)"
user-invocable: true
allowed-tools: Read, Glob, Grep, Edit
---

# 补一节读本

给一份**已经存在**的设计文档或便条补上 `## 读本`。

## 这不是写摘要

判据只有一条，而且很硬：

> **一个不做这块的开发者只读读本，应该完全懂这个系统怎么运转。**

读完还得回去翻规则表才明白，就是没写够。写成三五句导语，就是没做这件事。

完整的规则在 `rules/design-docs.md` 的「读本怎么写」那一节
（便条的在 `docs/team-memo-protocol.md`）。**先读那一份** ——
项目里那份 `.claude/rules/design-docs.md` 可能是旧的，
以插件里这一份为准。

## 步骤

### 1. 认清楚要改哪一篇

参数是仓库根目录起算的路径。读不到就说读不到，**不要去猜一个相近的文件名** ——
写进错的文件里，没有任何地方会报错。

### 2. 整篇读完，再动笔

**不许只读前几节就开始写。** 读本第 3 段要覆盖「每一条会影响玩家决策的规则」，
而那些规则散在 Detailed Rules、Edge Cases、Tuning Knobs 里。
漏掉一条的表现是读本看起来很完整、但少讲了一件事 —— 没有任何地方会报错。

顺便读一遍它依赖的上游文档（头部 `Dependencies` / `Implements Pillar` 指到的那些），
**只为了搞懂**，不要把它们的内容抄进来。

### 3. 已经有 `## 读本` 的话，先问

不许直接覆盖。摆出现在那一节，问是要重写还是要补哪一块。
**一份写过的读本可能是人改过的** —— 覆盖掉不会有任何提示。

### 4. 写

Full 文档五段，各用一个三级标题：

1. `### 这是个什么系统` —— 它在整个游戏里占什么位置，拿掉它游戏会缺什么
2. `### 玩家在里面经历什么` —— 从玩家视角走一遍：他看到什么、做什么决定、
   赢了输了分别是什么感受
3. `### 它怎么运转` —— **最长的一段，而且必须完整**。把规则用白话讲完，
   每条影响玩家决策的规则都要讲到，只是不写成条目和公式
4. `### 为什么这么定` —— 关键取舍。定成另一个样子会怎样、为什么没那么定
5. `### 一个完整的例子` —— 从头到尾走一遍具体的一局 / 一次交互，带上具体情境

Lite 文档写前三段（第 4 段并进第 3 段）。
便条不分段，篇幅跟着便条长度走：三十行的一两段，一百行那种四五段。

### 5. 三条硬规矩，写完自己对一遍

- **不许出现**：系统编号（`#20 §R7`）、类名 / 函数名、文件路径、
  里程碑代号（`PP-D5`、`FP gate`）、本篇之外没解释过的术语
- **不许写具体数值和公式。** 说「买通一个 AI 的价钱随着关卡推进变少」，
  不说「叛价 = 合作余利 × 0.6」。

  > 这条是**防走散**的，不是嫌数字难读。同一个数在两处各写一遍，
  > 改了其中一处**不会有任何地方报错**，而下游会照着过期的那份做下去。
  > **数值只有规则表那一份。**
- **不许省。** 「详见下文」「具体规则见 Detailed Rules」在读本里是失败信号 ——
  读本的读者正是那个不会往下翻的人

### 6. 插在哪儿

`## 读本` 是**正文第一节**：H1 和头部引用块之后，`## 设计前提` 之前。
便条里是 H1 之后、背景那段之前。

**只加这一节，别动别的。** 这次任务不是重构那篇文档 ——
顺手改了别处，用户在 diff 里就分不出哪些是他要的。

### 7. 交回去的时候说三件事

- 写了几段、多少字
- **读的时候发现的问题**（规则自相矛盾、缺了边界情况）—— 只报，不改
- 有没有哪一块你**没把握讲对**，具体是哪一块

最后一条最重要：读本要是把系统讲错了，读的人不会知道，
而他会拿这个错的理解去做事。**没把握的地方要说出来，不要写得像很确定。**
