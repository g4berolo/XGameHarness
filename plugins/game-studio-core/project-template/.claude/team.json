{
  "_comment": "身份注册表。resolve-identity.sh 用 git config user.name / user.email 匹配到 identity key，决定 team/session-state/{identity}/ 与 team/session-logs/{identity}/ 的路径。每个开发者把自己的 git 账号加进 identities；identity key 用小写字母/下划线（会成为目录名）。",
  "version": 1,
  "identities": {
    "<identity-key>": {
      "git_users": ["<git config user.name>"],
      "git_emails": ["<git config user.email>"],
      "role": "admin",
      "display_name": "<显示名>"
    }
  },
  "roles": {
    "admin": { "can_edit_team_config": true, "can_edit_all_sessions": true },
    "developer": { "can_edit_team_config": false, "can_edit_all_sessions": false },
    "artist": { "can_edit_team_config": false, "can_edit_all_sessions": false }
  }
}
