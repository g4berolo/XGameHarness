{
  "mcpServers": {
    "xgh-blender": {
      "command": "python",
      "args": ["-u", "-c", "import pathlib,runpy,sys; p=pathlib.Path(sys.argv[1]); p=p if p.is_absolute() and p.is_dir() else pathlib.Path.cwd(); runpy.run_path(str(p/'scripts'/'mcp_server.py'),run_name='__main__')", "${CLAUDE_PLUGIN_ROOT}"],
      "cwd": "."
    }
  }
}
