# Python 字节码 —— 校验 hook 脚本时（python -m py_compile）会生成
__pycache__/
*.py[cod]

# 本机私有覆盖
.claude/settings.local.json

# hook 运行时写的状态（rules-injected marker / 各类 .log）
.claude/state/

# 在本仓库里跑 session 时 hook 写的 session-state / session-log / memo。
# 游戏项目里这些是要入库的团队历史（project-template/.gitignore 故意不忽略），
# 但 harness 仓库自身不是游戏项目 —— 这里只是开发 harness 时的副产物。
/team/

# 读本实验室的运行产物（每轮的 digest / gdd / lint 输出 / 看法卡 / docshot 截图
# 和它造的 Chrome 用户目录）。结论已经收进 RUNLOG.md —— 入库的是结论和它落成的
# 规矩，不是每一轮的原始产物。要留档就单独归档，别进这个仓库。
labs/digest-lab/runs/

# archify 的 visual-check 证据：四档视口 × 明暗两套截图 + 回执。
# 每跑一次都重新生成，入库的是 .json 源、交付的 .html、和 README 用的那张 .png。
docs/diagrams/*.visual-check.*
